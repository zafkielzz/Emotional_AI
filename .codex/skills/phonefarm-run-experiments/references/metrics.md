# Required metrics

Store TTFT, latency per token, tokens/s, compute, serialization, network, deserialization, bytes, RAM, temperature, CPU load, queue length, and errors. Include the route mode and request ID in every event.
