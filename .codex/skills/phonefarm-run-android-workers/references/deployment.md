# Dashboard deployment

Use the web dashboard or its ADB terminal to install the generated APK. Start with controller relay: each worker opens outbound HTTP and TCP connections. Test phone-to-phone TCP only as an optional route after relay succeeds.
